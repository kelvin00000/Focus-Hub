import Navbar from "../../components/Navbar"

export default function SignInPage(){
    return(
        <>
            <title>Sign In</title>

            <Navbar title={"Sign In"} showMenuButton={false} />

            <section className="flex items-center justify-center w-full h-screen bg-bgdark">
                working
            </section>
        </>
    )
}
