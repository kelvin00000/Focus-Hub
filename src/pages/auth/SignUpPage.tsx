import Navbar from "../../components/Navbar"

export default function SignUpPage(){
    return(
        <>
            <title>Sign Up</title>

            <Navbar title={"Sign Up"} showMenuButton={false} />

            <section className="flex items-center justify-center w-full h-screen bg-bgdark">
                working
            </section>
        </>
    )
}
